1.对象创建过程(半初始化过程)
    申请空间
    调用构造方法
    指针引用
2.DCL单例需不需要加volatitle
    需要  对象创建过程(半初始化过程)需禁止指令重排序
    volatitle作用 可见性 禁止指令重排序
3.对象在内存中存储布局(jol可查看对象布局)
  8字节 markword 锁 hashcode gc信息
  4个字节 类型指针
  实例数据
  对其 补齐能被8整除
4.对象头包括什么
  markword 锁 hashcode gc信息    
  类型指针
5.对象怎么定位
    直接指针
6.对象怎么分配
    尝试分配在堆
    太大直接分配老年代 参数配置
    TLAB
    ······
7.Object在内存中占有多少字节
    16个字节
8.Class对象在堆还是方法区
    1.8 
        方法区oop引用堆的class对象信息
